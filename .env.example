# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

NEXT_PUBLIC_GOKEI_API_URL="https://staging.backend.getskip.ai/api/spot"
NEXT_PUBLIC_GOKEI_WIDGET_URL="https://staging.spot.getskip.ai"
NEXT_PUBLIC_GOKEI_PUBLIC_KEY="PK_EXAMPLE"

# CNPL (Care Now Pay Later) — SkipPay API base URL.
# Called directly from the browser; set as a build-time variable in the Cloudflare
# Pages dashboard (under Settings > Environment Variables).
NEXT_PUBLIC_SKIPPAY_API_URL="https://staging.pay.getskip.ai"
